[test]
# The same test files run under vitest (Node) and bun:test (Bun): they
# import from "vitest", which `bun test` remaps to bun:test. The preload
# boots one shared control plane per run — the bun equivalent of
# test/e2e-global-setup.ts.
root = "test"
preload = ["./test/bun-preload.ts"]
